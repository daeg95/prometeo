# Prometeo probe

## Development server

Install dependencies with npm install
Run `npm run startapp` for run the algorithm. 
Inside the prometeo_probe.js file on line 74 change the 139 in the funtion to the desired height



